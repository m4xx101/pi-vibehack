---
description: Branch the hypothesis tree into a side conversation (requires pi-side-chat)
model: claude-haiku-4-5
---

The user invoked /vibehack-fork. This command requires the pi-side-chat extension.

If pi-side-chat is detected, fork via its mechanism.

Otherwise, print:
> ⚠ /vibehack-fork requires the pi-side-chat extension. Install via: `npm i -g pi-side-chat`
